# Optional eBay API credentials for automated upload.
# Copy to .ebay-credentials.env and fill in after developer account is approved.
# The Monday automation uploads these to Cloudflare if not already configured.

EBAY_CLIENT_ID=
EBAY_CLIENT_SECRET=
EBAY_RUNAME=
